AWSTemplateFormatVersion: "2010-09-09"
Description: >-
  Securitybricks FedRAMP Assessment — Auditor Read-Only Role.
  Deploy this template in your AWS account to create a read-only IAM role
  for Securitybricks assessment engineers. The role uses SecurityAudit
  and targeted read-only policies — it cannot modify any resources.
  Assessors log in with their IAM user accounts and assume this role.

# ---------------------------------------------------------------------------
# Parameters
# ---------------------------------------------------------------------------
Parameters:
  RoleName:
    Type: String
    Default: SecuritybricksFedRAMPAuditorRole
    Description: Name for the IAM role (customize if needed)
    AllowedPattern: "^[a-zA-Z0-9_+=,.@\\-]+$"
    MaxLength: 64

  SessionDurationSeconds:
    Type: Number
    Default: 3600
    MinValue: 900
    MaxValue: 7200
    Description: Maximum session duration in seconds (15 min to 2 hours)

# ---------------------------------------------------------------------------
# Metadata (for CloudFormation console display)
# ---------------------------------------------------------------------------
Metadata:
  AWS::CloudFormation::Interface:
    ParameterGroups:
      - Label:
          default: Role Configuration
        Parameters:
          - RoleName
          - SessionDurationSeconds
    ParameterLabels:
      RoleName:
        default: IAM Role Name
      SessionDurationSeconds:
        default: Maximum Session Duration

# ---------------------------------------------------------------------------
# Resources
# ---------------------------------------------------------------------------
Resources:

  # =========================================================================
  # Auditor IAM Role (same-account assume-role)
  # =========================================================================
  FedRAMPAuditorRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Ref RoleName
      MaxSessionDuration: !Ref SessionDurationSeconds
      Description: >-
        Read-only role for Securitybricks FedRAMP assessment engineers.
        Grants access to audit AWS configurations for NIST 800-53 Rev 5
        control compliance without any write permissions.
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Sid: AllowSameAccountAssume
            Effect: Allow
            Principal:
              AWS: !Sub "arn:aws:iam::${AWS::AccountId}:root"
            Action: sts:AssumeRole
      ManagedPolicyArns:
        # SecurityAudit provides read access to security-related configurations
        # across IAM, CloudTrail, Config, GuardDuty, KMS, etc.
        - arn:aws:iam::aws:policy/SecurityAudit
      Tags:
        - Key: Project
          Value: FedRAMP-Assessment
        - Key: ManagedBy
          Value: CloudFormation
        - Key: Purpose
          Value: Securitybricks-FedRAMP-Assessment
        - Key: AccessLevel
          Value: ReadOnly

  # =========================================================================
  # Custom Policy — Additional Read-Only API Calls for FedRAMP Assessment
  #
  # These cover specific API calls needed for NIST 800-53 control validation
  # that are not included in the SecurityAudit managed policy.
  # =========================================================================
  FedRAMPAuditorAdditionalReadPolicy:
    Type: AWS::IAM::Policy
    Properties:
      PolicyName: SecuritybricksFedRAMPAuditorAdditionalRead
      Roles:
        - !Ref FedRAMPAuditorRole
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          # -----------------------------------------------------------------
          # IAM and Identity (FedRAMP: AC, IA domains)
          # -----------------------------------------------------------------
          - Sid: IAMCredentialReporting
            Effect: Allow
            Action:
              - iam:GenerateCredentialReport
              - iam:GetCredentialReport
              - iam:GetAccountAuthorizationDetails
              - iam:GetAccountPasswordPolicy
              - iam:ListServiceSpecificCredentials
              - iam:ListSigningCertificates
              - iam:ListMFADevices
              - iam:ListAccessKeys
              - iam:GetAccessKeyLastUsed
              - iam:ListUserPolicies
              - iam:ListAttachedUserPolicies
              - iam:ListGroupsForUser
              - iam:GetLoginProfile
            Resource: "*"

          # -----------------------------------------------------------------
          # Compute and Networking (FedRAMP: SC, CM domains)
          # -----------------------------------------------------------------
          - Sid: ComputeNetworkRead
            Effect: Allow
            Action:
              - ec2:DescribeInstances
              - ec2:DescribeSecurityGroups
              - ec2:DescribeSecurityGroupRules
              - ec2:DescribeNetworkAcls
              - ec2:DescribeFlowLogs
              - ec2:DescribeVpcs
              - ec2:DescribeSubnets
              - ec2:DescribeRouteTables
              - ec2:DescribeVpnConnections
              - ec2:DescribeVpcEndpoints
              - ec2:DescribeVolumes
              - ec2:DescribeSnapshots
              - ec2:DescribeImages
              - ec2:GetEbsEncryptionByDefault
              - ec2:DescribeInstanceAttribute
              - ec2:DescribeClientVpnEndpoints
              - ec2:DescribeVpcPeeringConnections
              - ec2:DescribeTransitGateways
              - ec2:DescribeTransitGatewayAttachments
              - ec2:DescribeNetworkInterfaces
              - ec2:DescribeNatGateways
              - ec2:DescribeSnapshotAttribute
              - elasticloadbalancing:DescribeLoadBalancers
              - elasticloadbalancing:DescribeListeners
              - elasticloadbalancing:DescribeTargetGroups
              - elasticloadbalancing:DescribeSSLPolicies
              - elasticloadbalancing:DescribeLoadBalancerAttributes
            Resource: "*"

          # -----------------------------------------------------------------
          # Storage (FedRAMP: MP, SC domains)
          # -----------------------------------------------------------------
          - Sid: StorageRead
            Effect: Allow
            Action:
              - s3:GetBucketPolicy
              - s3:GetBucketAcl
              - s3:GetBucketVersioning
              - s3:GetBucketLogging
              - s3:GetEncryptionConfiguration
              - s3:GetBucketPublicAccessBlock
              - s3:GetAccountPublicAccessBlock
              - s3:GetBucketPolicyStatus
              - s3:ListAllMyBuckets
              - s3:GetBucketLocation
              - s3:GetBucketTagging
              - s3:GetLifecycleConfiguration
              - s3:GetReplicationConfiguration
              - elasticfilesystem:DescribeFileSystems
            Resource: "*"

          # -----------------------------------------------------------------
          # Database (FedRAMP: SC, MP domains)
          # -----------------------------------------------------------------
          - Sid: DatabaseRead
            Effect: Allow
            Action:
              - rds:DescribeDBInstances
              - rds:DescribeDBClusters
              - rds:DescribeDBSnapshots
              - rds:DescribeDBClusterSnapshots
              - rds:DescribeDBSubnetGroups
              - rds:ListTagsForResource
              - rds:DescribeDBParameterGroups
              - rds:DescribeDBParameters
              - rds:DescribeEventSubscriptions
              - dynamodb:ListTables
              - dynamodb:DescribeTable
              - dynamodb:DescribeContinuousBackups
              - dynamodb:ListTagsOfResource
            Resource: "*"

          # -----------------------------------------------------------------
          # Encryption and Key Management (FedRAMP: SC domain)
          # -----------------------------------------------------------------
          - Sid: KMSRead
            Effect: Allow
            Action:
              - kms:ListKeys
              - kms:ListAliases
              - kms:DescribeKey
              - kms:GetKeyPolicy
              - kms:GetKeyRotationStatus
              - kms:ListResourceTags
            Resource: "*"

          # -----------------------------------------------------------------
          # Logging and Monitoring (FedRAMP: AU, SI domains)
          # -----------------------------------------------------------------
          - Sid: LoggingMonitoringRead
            Effect: Allow
            Action:
              - cloudtrail:DescribeTrails
              - cloudtrail:GetTrailStatus
              - cloudtrail:GetEventSelectors
              - cloudtrail:GetInsightSelectors
              - cloudtrail:LookupEvents
              - logs:DescribeLogGroups
              - logs:DescribeMetricFilters
              - cloudwatch:DescribeAlarms
              - cloudwatch:DescribeAnomalyDetectors
              - config:DescribeConfigRules
              - config:DescribeComplianceByConfigRule
              - config:DescribeConfigurationRecorders
              - config:DescribeConfigurationRecorderStatus
              - config:DescribeDeliveryChannels
              - guardduty:ListDetectors
              - guardduty:GetDetector
              - securityhub:GetEnabledStandards
              - securityhub:DescribeHub
              - securityhub:GetFindings
            Resource: "*"

          # -----------------------------------------------------------------
          # Incident Response (FedRAMP: IR domain)
          # -----------------------------------------------------------------
          - Sid: IncidentResponseRead
            Effect: Allow
            Action:
              - ssm:DescribeAutomationExecutions
              - ssm:ListDocuments
              - ssm:DescribeDocument
              - ssm:GetDocument
              - ssm:DescribeInstanceInformation
              - ssm:DescribePatchBaselines
              - ssm:DescribeInstancePatchStates
              - ssm:ListInventoryEntries
              - sns:ListTopics
              - sns:ListSubscriptionsByTopic
              - sns:GetTopicAttributes
              - events:ListRules
              - events:DescribeRule
              - events:ListTargetsByRule
            Resource: "*"

          # -----------------------------------------------------------------
          # Backup and Recovery (FedRAMP: CP domain)
          # -----------------------------------------------------------------
          - Sid: BackupRead
            Effect: Allow
            Action:
              - backup:ListBackupPlans
              - backup:GetBackupPlan
              - backup:ListBackupVaults
              - backup:DescribeBackupVault
              - backup:ListRecoveryPointsByBackupVault
              - backup:GetBackupVaultAccessPolicy
              - backup:ListRestoreJobs
              - backup:ListCopyJobs
            Resource: "*"

          # -----------------------------------------------------------------
          # Resilience Hub (FedRAMP: CP domain)
          # -----------------------------------------------------------------
          - Sid: ResilienceHubRead
            Effect: Allow
            Action:
              - resiliencehub:ListApps
              - resiliencehub:ListAppAssessments
              - resiliencehub:DescribeApp
            Resource: "*"

          # -----------------------------------------------------------------
          # Organizations and Account (FedRAMP: CA domain)
          # -----------------------------------------------------------------
          - Sid: OrganizationsRead
            Effect: Allow
            Action:
              - organizations:DescribeOrganization
              - organizations:ListAccounts
              - organizations:ListPolicies
              - organizations:DescribePolicy
              - account:GetAlternateContact
              - support:DescribeTrustedAdvisorChecks
              - support:DescribeTrustedAdvisorCheckResult
            Resource: "*"

          # -----------------------------------------------------------------
          # Security Services (FedRAMP: RA, SI domains)
          # -----------------------------------------------------------------
          - Sid: SecurityServicesRead
            Effect: Allow
            Action:
              - inspector2:BatchGetAccountStatus
              - inspector2:ListFindings
              - wafv2:ListWebACLs
              - wafv2:GetWebACL
              - network-firewall:ListFirewalls
              - network-firewall:ListFirewallPolicies
              - network-firewall:DescribeFirewallPolicy
              - access-analyzer:ListAnalyzers
            Resource: "*"

          # -----------------------------------------------------------------
          # Web, CDN, DNS, and Certificates (FedRAMP: SC domain)
          # -----------------------------------------------------------------
          - Sid: WebCDNDNSRead
            Effect: Allow
            Action:
              - cloudfront:ListDistributions
              - acm:ListCertificates
              - acm:DescribeCertificate
              - route53:ListHostedZones
              - route53:GetDNSSEC
              - apigateway:GET
              - ecr:DescribeRepositories
            Resource: "*"

          # -----------------------------------------------------------------
          # Identity Center, CI/CD, Athena, Health (FedRAMP: AC, CM, AU domains)
          # -----------------------------------------------------------------
          - Sid: IdentityCICDAnalyticsRead
            Effect: Allow
            Action:
              - sso:ListInstances
              - codepipeline:ListPipelines
              - codepipeline:GetPipeline
              - codeguru-reviewer:ListRepositoryAssociations
              - codeguru-reviewer:DescribeRepositoryAssociation
              - athena:ListNamedQueries
              - athena:ListWorkGroups
              - health:DescribeEvents
            Resource: "*"

  # =========================================================================
  # Explicit Deny — Ensure No Write Access
  # =========================================================================
  FedRAMPAuditorDenyWritePolicy:
    Type: AWS::IAM::Policy
    Properties:
      PolicyName: SecuritybricksFedRAMPAuditorDenyWrite
      Roles:
        - !Ref FedRAMPAuditorRole
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Sid: DenyAllMutatingActions
            Effect: Deny
            Action:
              - ec2:RunInstances
              - ec2:TerminateInstances
              - ec2:StopInstances
              - ec2:StartInstances
              - ec2:CreateSecurityGroup
              - ec2:DeleteSecurityGroup
              - ec2:AuthorizeSecurityGroupIngress
              - ec2:RevokeSecurityGroupIngress
              - s3:PutObject
              - s3:DeleteObject
              - s3:DeleteBucket
              - s3:PutBucketPolicy
              - rds:DeleteDBInstance
              - rds:CreateDBInstance
              - rds:ModifyDBInstance
              - iam:CreateUser
              - iam:DeleteUser
              - iam:CreateRole
              - iam:DeleteRole
              - iam:AttachRolePolicy
              - iam:DetachRolePolicy
              - iam:PutRolePolicy
              - iam:DeleteRolePolicy
              - iam:CreatePolicy
              - iam:DeletePolicy
              - lambda:CreateFunction
              - lambda:DeleteFunction
              - lambda:InvokeFunction
              - cloudformation:CreateStack
              - cloudformation:DeleteStack
              - cloudformation:UpdateStack
            Resource: "*"

# ---------------------------------------------------------------------------
# Outputs
# ---------------------------------------------------------------------------
Outputs:
  RoleArn:
    Description: >-
      Role ARN for assessors to assume. Provide this to the Securitybricks
      assessment team along with the IAM user credentials.
    Value: !GetAtt FedRAMPAuditorRole.Arn

  RoleName:
    Description: Name of the IAM role created
    Value: !Ref RoleName

  AssumeRolePolicy:
    Description: >-
      Attach this inline policy to each assessor IAM user to allow them
      to assume the auditor role.
    Value: !Sub >-
      {"Version":"2012-10-17","Statement":[{"Effect":"Allow","Action":"sts:AssumeRole","Resource":"${FedRAMPAuditorRole.Arn}"}]}

  SetupInstructions:
    Description: Next steps after deploying this template
    Value: !Sub >-
      Role created successfully. Next steps:
      1) Create IAM user accounts for each assessor.
      2) Attach the AssumeRole policy (see AssumeRolePolicy output) to each user.
      3) Provide the Role ARN (${FedRAMPAuditorRole.Arn}), IAM usernames,
         passwords, and access keys to the Securitybricks assessment team
         via a secure channel.
